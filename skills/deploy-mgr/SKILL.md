# Deploy Mgr - 远程服务部署管理工具

## 功能特点

- **统一管理**: 集中管理所有远程服务器上的项目部署
- **快速操作**: 一键启动、停止、重启远程服务
- **状态监控**: 实时查看服务运行状态
- **日志查看**: 远程查看服务运行日志
- **多种认证**: 支持密码和 SSH 密钥认证
- **数据持久化**: 本地自动保存部署配置

## 🤖 在 OpenClaw 中使用

### 自然语言调用示例

#### 基本操作

**列出所有部署**:
```
显示所有部署列表
列出所有已配置的部署
查看我的所有部署
```

**添加部署**:
```
添加部署：
- 项目名: myapp
- 服务器: ws.flys.fun
- 用户: root
- 部署路径: /var/www/myapp
- 启动命令: npm start
- 使用 SSH 密钥 ~/.ssh/id_rsa
```

```
帮我添加一个新部署，项目名称是 api-server，部署在 192.168.1.100，
用户是 deploy，路径是 /opt/api-server，启动命令是 ./start.sh，
使用密码认证
```

**查看部署详情**:
```
显示 myapp 的详细信息
查看 myapp 的配置
myapp 的部署信息是什么
```

**删除部署**:
```
删除 myapp 部署
移除 myapp
不要 myapp 了
```

**更新部署**:
```
更新 myapp 的服务器地址为 new-server.com
修改 myapp 的部署路径为 /var/www/new-path
```

#### 服务管理

**查看部署状态**:
```
查看 myapp 的状态
检查 myapp 是否在运行
myapp 正常吗
```

**启动服务**:
```
启动 myapp 服务
运行 myapp
把 myapp 跑起来
```

**停止服务**:
```
停止 myapp 服务
关闭 myapp
停掉 myapp
```

**重启服务**:
```
重启 myapp 服务
重启 myapp
让 myapp 重新启动
```

**完整重启流程**:
```
对 prod-api 做个滚动重启
先停止旧版本，再启动新版本的 prod-api
```

#### 日志查看

**查看日志**:
```
查看 myapp 的日志
显示 myapp 的运行日志
myapp 有什么错误
```

**指定行数**:
```
查看 myapp 的日志，最近 50 行
显示最近 200 行日志
只看最后 100 行
```

**查看错误**:
```
查看 myapp 的错误日志
查看最近的错误信息
有没有报错
```

#### 批量操作

**批量查看状态**:
```
检查所有服务的状态
api、web、worker 这几个服务都怎么样了
```

**批量重启**:
```
重启所有服务
重新启动 api 和 web服务
把所有应用都重启一下
```

#### 场景示例

**场景 1: 首次部署配置**
```
你: 帮我配置生产环境的部署

OpenClaw: 好的，请提供以下信息：
         1. 项目名称
         2. 服务器地址
         3. 用户名
         4. 部署路径
         5. 启动命令
         6. 停止命令
         7. 日志路径
         8. 认证方式

你: 项目名是 prod-api，服务器 ws.flys.fun，用户 root，路径 /var/www/prod-api，
    启动命令 pm2 start app.js，停止命令 pm2 stop app，日志在 /var/log/prod-api/out.log，
    用 SSH 密钥 ~/.ssh/id_rsa

OpenClaw: ✓ 部署配置成功！
         项目: prod-api
         服务器: ws.flys.fun
         路径: /var/www/prod-api
```

**场景 2: 日常运维**
```
你: 生产环境的 api 状态怎么样？

OpenClaw: 检查中...
         ================================================================================
         Status: prod-api
         ================================================================================
         ✅ Status:  RUNNING
         📝 Message: Found 1 process(es) matching 'prod-api'
         ================================================================================

你: 查看一下最近的日志

OpenClaw: ================================================================================
         Logs: prod-api
         File: /var/log/prod-api/out.log
         ================================================================================
         [INFO] Server started on port 3000
         [INFO] Connected to database
         [INFO] Ready to accept connections
         ================================================================================
```

**场景 3: 故障处理**
```
你: 我的 web 应用好像挂了，帮我重启一下

OpenClaw: 正在重启 web 服务...
         ================================================================================
         Restart Service: web
         ================================================================================
         ✅ Service restarted successfully!
         📝 Message: Service stopped and started
         ================================================================================

         已重启 web 服务，现在的状态：
         ================================================================================
         Status: web
         ================================================================================
         ✅ Status:  RUNNING
         📝 Message: Found 1 process(es) matching 'web'
         ================================================================================
```

**场景 4: 日志排查**
```
你: api 服务最近有报错吗？查看一下最近的错误

OpenClaw: 正在查看 api 服务日志（最近 100 行）...
         ================================================================================
         Logs: api
         File: /var/log/api/error.log
         ================================================================================
         [ERROR] 2026-02-20 10:15:32 - Connection timeout
         [ERROR] 2026-02-20 10:16:45 - Database connection lost
         ================================================================================
         发现 2 条错误记录，主要是连接问题
```

**场景 5: 批量管理**
```
你: 列出所有部署，然后帮我把 api、web、worker 都重启一遍

OpenClaw: ================================================================================
         ID    Name                 Host                      Path
         ================================================================================
         1     api                  ws.flys.fun               /var/www/api
         2     web                  ws.flys.fun               /var/www/web
         3     worker               ws.flys.fun               /var/www/worker

         Total: 3 deployment(s)

         正在依次重启...

         ✓ api 重启成功
         ✓ web 重启成功
         ✓ worker 重启成功

         所有服务已重启完成！
```

**场景 6: 紧急部署**
```
你: 刚上线的新版本有问题，帮我回滚重启一下旧版本

OpenClaw: 好的，正在重启 prod-api 服务...
         ================================================================================
         Restart Service: prod-api
         ================================================================================
         ✅ Service restarted successfully!
         📝 Message: Services restarted
         ================================================================================

         服务已重启。检查状态中...

         ================================================================================
         Status: prod-api
         ================================================================================
         ✅ Status:  RUNNING
         📝 Message: Service is healthy
         ================================================================================

         回滚完成，服务正常运行。
```

### OpenClaw 工作原理

当你在 OpenClaw 对话中描述部署管理需求时：

1. **识别意图**: OpenClaw 识别出你想要管理远程部署
2. **调用技能**: 自动调用 Deploy Mgr 技能
3. **解析参数**: 从自然语言中提取关键信息
4. **执行操作**: 连接远程服务器并执行命令
5. **返回结果**: 告诉你操作是否成功
6. **后续跟进**: 根据结果提供进一步建议

### 对话式交互

OpenClaw 支持多轮对话，可以分步骤完成复杂操作：

**逐步添加部署示例**:
```
你: 有什么部署吗？

OpenClaw: 目前还没有配置部署，你需要添加一个吗？

你: 好的，帮我添加

OpenClaw: 好的，请告诉我：
         1. 项目名称（比如：api-server）
         2. 服务器地址

你: 项目叫 test-app，服务器是 ws.flys.fun

OpenClaw: 收到。还需要：
         3. 用户名
         4. 部署路径

你: 用户 root，路径 /var/www/test-app

OpenClaw: 最后几个信息：
         5. 启动命令
         6. 认证方式（密码或 SSH 密钥）

你: 启动命令是 python app.py，使用 SSH 密钥 ~/.ssh/id_rsa

OpenClaw: ✓ 部署添加成功！
         - 项目: test-app
         - 服务器: ws.flys.fun
         - 路径: /var/www/test-app

         现在可以启动它了，需要现在就启动吗？
```

### 典型对话示例

**示例 1: 添加新部署（一站式）**
```
你: 帮我添加一个部署

OpenClaw: 好的，请提供以下信息：
         1. 项目名称
         2. 服务器地址
         3. 用户名
         4. 部署路径
         5. 启动命令
         6. 认证方式（密码或SSH密钥）

你: 项目名是 test-api，服务器是 ws.flys.fun，用户 root，部署路径
    /var/www/test-api，启动命令是 python app.py，使用 SSH 密钥

OpenClaw: ✓ 部署添加成功！
         ID: 1
         名称: test-api
```

**示例 2: 查看和管理（多步操作）**
```
你: 显示所有部署

OpenClaw: ================================================================================
         ID    Name                 Host                      Path                          Description
         ================================================================================
         1     test-api             ws.flys.fun               /var/www/test-api             测试API服务

         Total: 1 deployment(s)

你: 启动 test-api

OpenClaw: ================================================================================
         Start Service: test-api
         ================================================================================
         ✅ Service started successfully!
         📝 Message: Server started on port 3000
         ================================================================================
```

## 💻 命令行使用

### 安装依赖

```bash
pip install paramiko click
```

### 基本命令

```bash
# 列出所有部署
deploy-mgr list

# 添加部署
deploy-mgr add \
  --name myapp \
  --host server.com \
  --username deploy \
  --auth-type key \
  --auth-data ~/.ssh/id_rsa \
  --deploy-path /var/www/myapp \
  --start-command "npm start" \
  --stop-command "npm stop" \
  --log-path /var/log/myapp/app.log

# 查看部署详情
deploy-mgr info myapp

# 查看状态
deploy-mgr status myapp

# 启动服务
deploy-mgr start myapp

# 停止服务
deploy-mgr stop myapp

# 重启服务
deploy-mgr restart myapp

# 查看日志
deploy-mgr logs myapp
deploy-mgr logs myapp --lines 50

# 更新部署
deploy-mgr update myapp --host new-server.com

# 删除部署
deploy-mgr remove myapp
```

## 🚀 OpenClaw 用户快速指南

### 参数说明（自然语言识别）

| 参数 | 自然语言关键词 | 示例 |
|------|---------------|------|
| 项目名称 | 项目名/服务名/应用名 | "项目名是 myapp" |
| 服务器地址 | 服务器/主机/地址 | "服务器 ws.flys.fun" |
| 用户名 | 用户/用户名 | "用户 root" |
| 部署路径 | 部署路径/项目路径 | "部署路径 /var/www" |
| 启动命令 | 启动命令/运行命令 | "启动命令 npm start" |
| 停止命令 | 停止命令 | "停止命令 npm stop" |
| 日志路径 | 日志路径/日志文件 | "日志路径 /var/log/app.log" |
| SSH 密钥 | SSH 密钥/密钥文件 | "使用 SSH 密钥 ~/.ssh/id_rsa" |

### 常见操作模式

**添加部署**:
```
添加 [项目名] 部署到 [服务器]，用户 [用户名]，路径 [路径]，启动命令 [命令]
```

**查看状态**:
```
查看 [项目名] 的状态
```

**启停服务**:
```
启动/停止/重启 [项目名]
```

**查看日志**:
```
查看 [项目名] 的日志
```

### 提示和技巧

1. **简洁表达**: 信息越明确，识别越准确
2. **使用项目名**: 通过项目名引用部署最方便
3. **提前配置**: 建议在启动命令和停止命令中包含完整的执行路径
4. **日志文件**: 配置日志路径可以快速查看问题

## 📝 使用场景

### 场景1: 开发环境部署

```bash
 deploy-mgr add \
   --name dev-api \
   --host dev-server.com \
   --username dev \
   --auth-type key \
   --auth-data ~/.ssh/dev_key \
   --deploy-path /home/dev/api \
   --start-command "pm2 start app.js" \
   --stop-command "pm2 stop app" \
   --log-path /var/log/dev-api/app.log
```

### 场景2: 生产环境管理

```bash
# 列出所有服务
deploy-mgr list

# 检查服务状态
deploy-mgr status prod-api

# 滚动重启
deploy-mgr restart prod-api

# 查看错误日志
deploy-mgr logs prod-api --lines 200
```

### 场景3: 批量管理

可以配合脚本批量操作多个部署：

```bash
# 批量启动
for name in api web worker; do
  deploy-mgr start $name
done

# 批量检查状态
for name in api web worker; do
  deploy-mgr status $name
done
```

## 🔧 配置和认证

### 认证方式

#### 1. SSH 密钥认证（推荐）
```bash
deploy-mgr add \
  --auth-type key \
  --auth-data ~/.ssh/id_rsa \
  ...
```

#### 2. 密码认证
```bash
deploy-mgr add \
  --auth-type password \
  --auth-data "your_password" \
  ...
```

### 数据存储

部署数据存储在：
- **Windows**: `C:\Users\<用户名>\.deploy-mgr\deployments.db`
- **Linux/Mac**: `~/.deploy-mgr/deployments.db`

### 安全提示

1. ⚠️ 密码会明文存储在本地数据库中，建议使用 SSH 密钥认证
2. 🔐 确保 `.deploy-mgr` 目录权限正确
3. 👤 定期审查部署配置

## 📊 数据结构

### 部署记录

```json
{
  "id": 1,
  "name": "myapp",
  "host": "server.com",
  "port": 22,
  "username": "deploy",
  "auth_type": "key",
  "auth_data": "/home/user/.ssh/id_rsa",
  "deploy_path": "/var/www/myapp",
  "start_command": "npm start",
  "stop_command": "npm stop",
  "status_command": null,
  "log_path": "/var/log/myapp/app.log",
  "description": "生产环境应用",
  "created_at": "2026-02-20 10:00:00",
  "updated_at": "2026-02-20 10:00:00"
}
```

## 🛠️ 故障排查

### 问题1: 连接失败
- ✅ 检查服务器地址和端口
- ✅ 确认 SSH 服务运行中
- ✅ 验证认证信息正确

### 问题2: 命令执行失败
- ✅ 检查部署路径是否存在
- ✅ 验证命令在服务器上可用
- ✅ 查看详细错误信息

### 问题3: 日志文件未找到
- ✅ 确认日志路径配置正确
- ✅ 检查日志文件权限
- ✅ 验证日志文件存在

## 📌 常见问题

**Q: 如何修改已添加的部署？**
```bash
deploy-mgr update myapp --new-option value
```

**Q: 如何备份配置？**
直接复制 `~/.deploy-mgr/` 目录即可。

**Q: 支持批量操作吗？**
可以通过 shell 脚本实现批量操作。

**Q: 如何查看操作历史？**
数据库中的 `operation_logs` 表记录了所有操作。

---

**版本**: 1.0.0
**开发者**: OpenClaw
**许可证**: MIT
